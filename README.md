# Suspend Resume Plugin & Widget

## Installing the Widget

1. Create a ZIP file containing the `widget` directory:

   ```bash
   zip suspend-resume.zip widget/* -x widget/src/
   ```
   
2. Upload the Widget to the manager.
